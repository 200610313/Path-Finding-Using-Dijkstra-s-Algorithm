/**
 * Represents each node of the Binary Search Tree
 *
 * Every time an establishment is added to the list,
 * it is also added as a Node in the binary search tree,
 * with its name and vertex location
 */
public class Node {
    String data;
    int loc;
    Node left;
    Node right;
}
